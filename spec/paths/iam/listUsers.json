{
  "tags": [
    "user",
    "severe"
  ],
  "summary": "Retrieves all users on the platform\n",
  "description": "Retrieves information of the platform users, may contain sensitive information.\n",
  "operationId": "listUsers",
  "parameters": [
    {
      "name": "username",
      "in": "query",
      "description": "Performs a case-insensitive full text search of usernames.",
      "required": false,
      "type": "string"
    },
    {
      "name": "email",
      "in": "query",
      "description": "Performs a case-insensitive full text search of e-mails.",
      "required": false,
      "type": "string"
    },
    {
      "name": "limit",
      "in": "query",
      "description": "Limits the number of entities found in response.",
      "required": false,
      "type": "integer",
      "default": 20,
      "minimum": 1,
      "maximum": 40
    },
    {
      "name": "offset",
      "in": "query",
      "description": "Skips given number of entities to provide pagination.",
      "required": false,
      "type": "integer",
      "default": 0,
      "minimum": 0
    }
  ],
  "responses": {
    "200": {
      "description": "Users are returned in an array.",
      "schema": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "integer",
              "format": "int64"
            },
            "username": {
              "type": "string"
            },
            "primary_email": {
              "type": "string",
              "description": "Primary email address of the user."
            },
            "personal_information": {
              "type": "object",
              "properties": {
                "given_name": {
                  "type": "string",
                  "description": "A name given to a individual to differentiate them from members of such group or family. May be referred as \"family name\" or \"forename\"."
                },
                "middle_name": {
                  "type": "string",
                  "description": "A name given to a individual to differentiate them from members of such group or family with members having same given name."
                },
                "family_name": {
                  "type": "string",
                  "description": "A name given to a individual to represent its family or group."
                },
                "name_prefix": {
                  "type": "string",
                  "description": "Prefix of the name to vocal the individual, such as \"Mr.\"."
                },
                "name_postfix": {
                  "type": "string",
                  "description": "Postfix of the name to vocal the individual, such as \"-san\", which may expand to something like \"Suguro-san\"."
                },
                "nickname": {
                  "type": "string",
                  "description": "A nick given to an individual, generally pronounced informally."
                },
                "image": {
                  "type": "string",
                  "description": "Image URL of a user."
                }
              }
            }
          }
        }
      }
    }
  }
}
