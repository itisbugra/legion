{
  "type": "object",
  "description": "Rate limiting information of the authenticated user.\n",
  "properties": {
    "threshold": {
      "type": "integer",
      "format": "int32",
      "description": "Calls per day allowed to the user."
    },
    "remaining": {
      "type": "integer",
      "format": "int32",
      "description": "Remaining calls available to the user."
    }
  }
}
