{
  "tags": [
    "user",
    "mailing",
    "severe"
  ],
  "summary": "Makes the given email address entry primary\n",
  "description": "Sends a confirmation mail containing a link to make the email entry primary.",
  "operationId": "makeEmailPrimary",
  "parameters": [
    {
      "name": "user_id",
      "in": "path",
      "required": true,
      "description": "Identifier of the user.",
      "type": "integer",
      "format": "int64"
    },
    {
      "name": "entry_id",
      "in": "path",
      "required": true,
      "description": "Identifier of the email entry.",
      "type": "integer",
      "format": "int64"
    },
    {
      "name": "force",
      "in": "query",
      "required": true,
      "description": "Forces the remote procedure to make the given email entry primary without sending a confirmation mail.",
      "type": "boolean"
    }
  ],
  "responses": {
    "200": {
      "description": "Action has been successfully completed."
    },
    "404": {
      "description": "User or entry not found. Refer to the response."
    }
  }
}
