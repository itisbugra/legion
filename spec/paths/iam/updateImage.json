{
  "tags": [
    "user",
    "mediocre"
  ],
  "summary": "Replaces the image of the user\n",
  "description": "Adds the given image to the platform database, replacing if it was existent. Image is expected to be in JPEG or PNG format, and its size should be lower than 256 KiB.",
  "operationId": "updateImage",
  "accepts": "image/*",
  "parameters": [
    {
      "name": "id",
      "in": "path",
      "required": true,
      "description": "Identifier of the platform user.",
      "type": "integer",
      "format": "int64"
    },
    {
      "name": "image",
      "in": "body",
      "required": true,
      "description": "Binary image data.",
      "type": "string",
      "format": "binary"
    }
  ]
}
