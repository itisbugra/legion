{
  "tags": [
    "user",
    "safe",
    "telephony"
  ],
  "summary": "Removes a phone number",
  "description": "Removes a phone number of a user from the platform.",
  "operationId": "addPhoneNumber",
  "parameters": [
    {
      "name": "user_id",
      "in": "path",
      "required": true,
      "description": "Identifier of the platform user.",
      "type": "integer",
      "format": "int64"
    },
    {
      "name": "phone_number_id",
      "in": "path",
      "required": true,
      "description": "Identfier of the phone number.",
      "type": "integer",
      "format": "int64"
    }
  ],
  "responses": {
    "204": {
      "description": "Phone number has been removed from the platform."
    }
  }
}
